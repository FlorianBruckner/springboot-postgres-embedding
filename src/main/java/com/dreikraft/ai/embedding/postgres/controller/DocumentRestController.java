package com.dreikraft.ai.embedding.postgres.controller;

/**
 * REST API removed in favor of MVC-only UI flow (see ViewController).
 */
final class DocumentRestController {
    private DocumentRestController() {
    }
}
